package oauthserver

import (
// "strings"
)

// func (o *OryClient) AddScope(id string, scope string) {
// 	client := o.GetClient(id)
// 	scope = *client.Scope + " " + scope
// 	o.PatchClient(id, "replace", "/scope", scope)
// }

// func (o *OryClient) RemoveScope(id string, scope string) {
// 	client := o.GetClient(id)
// 	scope = strings.Replace(*client.Scope, scope, "", -1)
// 	o.PatchClient(id, "replace", "/scope", scope)
// }
